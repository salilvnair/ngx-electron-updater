export enum DefaultDownloadInfo {
    suffix = "ngxeu/updates/pending",
    newUpdateBuildPath="ngxeu/updates/pending/build",
    newUpdateAppPath="ngxeu/updates/pending/app",
    buildPath="build",
    appPath=".",
    extractPath = "ngxeu/updates/pending"
};

export enum Path {
    separator = "/"
}